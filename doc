React从后台拿到字符串类型的标签如何插到render中。。？
方法一： <div dangerouslySetInnerHTML={{__html: `从后台拿到字符串类型的标签`}} />
方法二： json，array类型 render(){
  return jsonList.map((从后台拿到字符串类型的标签, index) => {
    return <div className="tag" key={index}>{从后台拿到字符串类型的标签}</div>
  })
}
