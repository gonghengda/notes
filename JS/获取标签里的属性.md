 let li = document.querySelectorAll('.address li');
    li.forEach((item, index) => {
      if (id === index) {
        this.setState({ value: item.getAttribute('value'), options: [] });
      }
    });
